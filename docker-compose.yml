services:
  nginx:
    image: nginx
    ports: 
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf

  backend:
   image: hashicorp/http-echo
   command: ["-text= Hello world from backend"]
